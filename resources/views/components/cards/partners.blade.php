<div class="partners__card">
    <div class="partners__card-logo">
        <img src="{{ Storage::url($item->logo) }}" alt="{{ $item->name }}" class="partners__card-image">
    </div>
    <h3 class="partners__card-title">{{ $item->name }}</h3>
</div>
